import { ArticleModelUpdate } from './article-model-update';

describe('ArticleModelUpdate', () => {
  it('should create an instance', () => {
    expect(new ArticleModelUpdate()).toBeTruthy();
  });
});
